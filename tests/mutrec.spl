// They should by typed as:
// f :: ∀a.a->a
// g :: ∀a.a->a
// If you type too much as once, they are typed as: (which is wrong)
// f :: ∀ab.a->b
// g :: ∀ab.a->b

f (x) {
	return g (x);
}

g (x) {
	return f (x);
}

main () {
	print(g(42));
}
